# ===========================
# Configuración de la aplicación
# ===========================
spring.application.name=northFit
server.port=8080

# ===========================
# Configuración de la base de datos H2 (para desarrollo)
# ===========================
spring.datasource.url=jdbc:h2:mem:northfitdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ===========================
# Configuración de JPA/Hibernate
# ===========================
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===========================
# Configuración de Thymeleaf
# ===========================
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# ===========================
# Configuración para PostgreSQL (Producción)
# Descomenta estas líneas cuando uses PostgreSQL
# ===========================
#spring.datasource.url=jdbc:postgresql://localhost:5432/northfit
#spring.datasource.username=tu_usuario
#spring.datasource.password=tu_password
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===========================
# Configuración para MariaDB (Alternativa)
# Descomenta estas líneas cuando uses MariaDB
# ===========================
#spring.datasource.url=jdbc:mariadb://localhost:3306/northfit
#spring.datasource.username=tu_usuario
#spring.datasource.password=tu_password
#spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# ===========================
# Logging
# ===========================
logging.level.org.springframework.security=DEBUG
logging.level.proyectoWeb.northFit=DEBUG

# ===========================
# Configuración de archivos estáticos
# ===========================
spring.web.resources.static-locations=classpath:/static/